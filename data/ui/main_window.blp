using Gtk 4.0;
using Adw 1;

template $MainWindow : Adw.ApplicationWindow {
  title: "PackageSearch";
  default-width: 900;
  default-height: 640;
  width-request: 360;
  height-request: 480;

  content: Adw.NavigationView nav_view {};
}
